interface:
  display_name: "Oryh Business Object Summary"
  short_description: "Summarize business objects of one type (e.g. daily reports) for a manager"
  default_prompt: "Use $oryh-business-object-summary to read and summarize business objects with base_url and api_key."

policy:
  allow_implicit_invocation: true
